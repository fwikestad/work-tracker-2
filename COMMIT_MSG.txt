feat: Phase 1 MVP — Tauri 2 + Svelte 5 + SQLite full build

Backend (Chewie):
- Tauri 2 project scaffold (package.json, vite.config.ts, svelte.config.js)
- rusqlite + Mutex<Connection> with WAL mode crash safety
- Full SQLite schema: customers, work_orders, time_sessions, active_session, recent_work_orders
- 18 Tauri IPC commands across customers, work_orders, sessions, reports
- Atomic session switching service (stop old + start new in one transaction)
- Crash recovery: orphan session detection on startup
- quick_add: create customer + work order + start session atomically
- CSV export, daily summary, recent work orders queries
- TypeScript types + typed invoke() wrappers for all commands

Frontend (Leia):
- Svelte 5 runes-based stores: timer, sessions, ui state
- Timer component: active session, real-time elapsed, stop with notes/activity
- QuickAdd overlay: Ctrl+N, create customer+workorder+start in one action
- SearchSwitch: recent items + live search, arrow key navigation, Ctrl+K
- SessionList: today's sessions with inline edit (notes, duration override)
- DailySummary: total hours + customer breakdown, real-time updates
- RecoveryDialog: blocking orphan session recovery on startup
- CustomerList + WorkOrderList management pages
- Global keyboard shortcuts (Ctrl+N, Ctrl+K, Ctrl+S, Escape)
- Dark theme (#0d0d0d bg, #4caf7d accent) matching UI mockup

Docs (Mon Mothma):
- README.md: prerequisites, getting started, keyboard shortcuts, project structure
- docs/api-reference.md: all 18 IPC commands with TypeScript signatures

Tests (Wedge):
- docs/test-plan.md: 118 test cases (P0/P1/P2), 5 critical findings

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
